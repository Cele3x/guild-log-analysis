#!/usr/bin/env python3
"""Entry point script for Guild Log Analysis CLI."""

import os
import sys

# Add src directory to Python path
sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(__file__)), "src"))

from guild_log_analysis.cli import main  # noqa: E402

if __name__ == "__main__":
    main()
